namespace labo_10.Infrastructure.Services;

/*public class NotificationService
{
    private static int _attemptCount = 0;

    public void SendNotification(string user)
    {
        _attemptCount++;

        Console.WriteLine($"[Intento {_attemptCount}] Enviando notificación a {user} - {DateTime.Now}");

        // Simular fallo en los primeros 2 intentos
        if (_attemptCount <= 2)
        {
            Console.WriteLine($"Fallo simulado en intento {_attemptCount}");
            throw new Exception($"Error simulado en intento {_attemptCount}");
        }

        Console.WriteLine($"Notificación enviada correctamente a {user}");
    }
}*/
